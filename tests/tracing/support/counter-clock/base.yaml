native-byte-order: le
clock-types:
  default:
    origin-is-unix-epoch: false
    $c-type: uint64_t
data-stream-types:
  default:
    $is-default: true
    $default-clock-type-name: default
    event-record-types:
      dummy:
        payload-field-type:
          class: struct
          members:
            - u:
                field-type:
                  class: str
